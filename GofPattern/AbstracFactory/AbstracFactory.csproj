<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>disable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Excluded from the original (pre-SDK) project as well: references
         CJavaCourse/CNetCourse types that do not exist, so it does not compile. -->
    <Compile Remove="AbstractFactoryDemo\CorporateCourseFactory.cs" />
  </ItemGroup>

</Project>
